// This is file for some useful functions for all app

import { ElementRef } from "@angular/core";

// Use in keyvalue pipe for disabling default sort of this pipe
export const setWithoutSort = () => {
  return 0;
};
